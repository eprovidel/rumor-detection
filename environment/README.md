# Environment

The package versions described in the `vastai.yml` are the ones used to create
a conda environment on which the experiments were ran.

In conda this environment can be recreated by executing:

```bash
conda env create -f vastai.yml
```
